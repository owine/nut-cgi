{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "github>owine/renovate-config",
    "github>owine/renovate-config:automerge",
    "github>owine/renovate-config:docker",
    "github>owine/renovate-config:alpine"
  ],
  "commitBody": "Update {{depName}} from {{currentVersion}} to {{newVersion}}\n\n{{#if releases}}Release notes:\n{{#each releases}}- {{#if title}}{{title}}{{else}}{{version}}{{/if}}\n{{/each}}{{/if}}",
  "docker-compose": {
    "description": "Exclude the example compose file from Renovate. Scoped under the docker-compose manager rather than set at top level: a top-level ignorePaths REPLACES the list inherited from :ignoreModulesAndTests (node_modules, vendor, tests, etc.) instead of merging with it",
    "ignorePaths": [
      "docker-compose.yml"
    ]
  },
  "vulnerabilityAlerts": {
    "assignees": ["owine"]
  },
  "customManagers": [
    {
      "customType": "regex",
      "description": "Track NUT (Network UPS Tools) releases from GitHub",
      "managerFilePatterns": [
        "/Dockerfile$/"
      ],
      "matchStrings": [
        "# NUT version to build from source\\s+ARG NUT_VERSION=(?<currentValue>.*?)\\n"
      ],
      "datasourceTemplate": "github-releases",
      "depNameTemplate": "networkupstools/nut",
      "extractVersionTemplate": "^v(?<version>.*)$"
    }
  ],
  "packageRules": [
    {
      "description": "Alpine apk pins are extracted by Renovate's built-in dockerfile manager (>= 44.96.0) as datasource `apk`; the :alpine preset supplies the grouping, rangeStrategy and automerge posture but stays release-line agnostic, so this URL is the ONE place the Alpine release line is written. Bump `branch=v3.24` when the base image crosses a release line (3.24 -> 3.25). `components=main,community` merges both indexes in a single lookup (registryStrategy 'merge'), so community packages need no separate registryUrls override.",
      "matchDatasources": ["apk"],
      "registryUrls": ["https://dl-cdn.alpinelinux.org/alpine?branch=v3.24&components=main,community&arch=x86_64"]
    },
    {
      "description": "NUT source version — manual review on every bump, whatever the update type. The image builds NUT from source, so a bump is a rebuild, not a tag swap. Commit type is left to the fleet baseline (`deps:` => patch release); the rule below re-types the bumps that deserve more.",
      "matchDatasources": ["github-releases"],
      "matchPackageNames": ["networkupstools/nut"],
      "groupName": "nut source",
      "automerge": false,
      "addLabels": ["needs-review"]
    },
    {
      "description": "The ONE `feat:` in this repo's Renovate surface, and a deliberate exception to the fleet rule that no dependency update is ever typed `feat:` (see owine/renovate-config's README). It holds here because this repo is a packaging shim: its whole output is NUT, so the version a user runs IS this dependency's version — the question Renovate can't normally answer (\"is this bump consumer-visible?\") has a fixed answer of yes. Scoped to minor/major only so an upstream PATCH stays `deps:` (patch release) instead of cutting a minor here: the wrapper's semver then tracks upstream's own update type rather than drifting a digit ahead of it. Not `feat!:` on major — an upstream major is not automatically breaking for this image's users.",
      "matchDatasources": ["github-releases"],
      "matchPackageNames": ["networkupstools/nut"],
      "matchUpdateTypes": ["minor", "major"],
      "semanticCommitType": "feat"
    }
  ]
}
