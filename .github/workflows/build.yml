---
name: Build, Test, and Publish

"on":
  push:
    branches: ["main"]
    tags: ["v*.*.*"]
    paths-ignore:
      - "docs/**"
      - "README.md"
      - "CLAUDE.md"
      - "LICENSE"
      - ".github/renovate.json"
      - "CHANGELOG.md"
      - ".release-please-manifest.json"
      - "release-please-config.json"
      - "version.txt"
  # Run on every PR (no paths-ignore): the branch ruleset requires `ci-pass`
  # to report, and a workflow-level paths-ignore skips the whole run so the
  # aggregator never reports — leaving the PR permanently BLOCKED. Filtering
  # now lives in the `changes` job below, which gates the heavy `build` job;
  # docs-only PRs let `build` skip and `ci-pass` still passes.
  pull_request:
    branches: ["main"]
  workflow_dispatch:
    inputs:
      force-rebuild:
        description: 'Force rebuild without cache'
        required: false
        type: boolean
        default: false

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ !startsWith(github.ref, 'refs/tags/') }}

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  # Detect whether the change set touches image-affecting files. Used to gate
  # the heavy multi-arch `build` job on PRs so docs/spec/release-please-only
  # PRs skip the build while `ci-pass` still passes.
  #
  # This job ALWAYS runs (no top-level `if:`). It must not be skipped, because
  # GitHub propagates a skipped job's status transitively down the `needs`
  # chain: a skipped `changes` would skip `build`, `merge`, and everything
  # after — silently, since `ci-pass` treats skips as success. (That is the
  # #57 regression: making `changes` PR-only stopped all push/tag publishing.)
  # On non-PR events (push, tag, manual workflow_dispatch) the paths-filter is
  # pointless, so the `force` step short-circuits to image=true; the filter
  # only runs on PRs. These two branches are exhaustive over the configured
  # `on:` triggers — every event either is a `pull_request` (filter) or is not
  # (force). If a PR-like trigger is ever added (e.g. pull_request_target),
  # add it to the filter's `if:` so `image` is never left empty (empty ==
  # silent skip, the very failure mode this job exists to prevent).
  changes:
    runs-on: ubuntu-24.04
    outputs:
      image: ${{ steps.filter.outputs.image || steps.force.outputs.image }}
    steps:
      - name: Force build on non-PR events (push, tag, manual dispatch)
        id: force
        if: github.event_name != 'pull_request'
        run: echo "image=true" >> "$GITHUB_OUTPUT"
      - if: github.event_name == 'pull_request'
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - if: github.event_name == 'pull_request'
        uses: dorny/paths-filter@7b450fff21473bca461d4b92ce414b9d0420d706 # v4.0.2
        id: filter
        with:
          # `every`: a changed file matches a filter only if it satisfies ALL
          # rules. Without this, dorny's default OR semantics make the leading
          # `**` match everything, so the `!`-exclusions are no-ops and the
          # filter is always true. With `every`, a file must match `**` AND
          # none of the `!` excludes.
          predicate-quantifier: 'every'
          filters: |
            image:
              - '**'
              - '!docs/**'
              - '!**/*.md'
              - '!LICENSE'
              - '!.github/renovate.json'
              - '!.release-please-manifest.json'
              - '!release-please-config.json'
              - '!version.txt'

  build:
    name: Build ${{ matrix.platform }}
    # `changes` always succeeds and emits image=true on push/tag, so a plain
    # success-gated condition works here: no skipped dependency to propagate,
    # no status function needed. Skips only on docs-only PRs (image=false).
    needs: changes
    if: needs.changes.outputs.image == 'true'
    # Native per-arch runners eliminate QEMU emulation of the arm64 NUT C
    # compile, which dominated wall-clock under the old single-job
    # multi-platform build. amd64 builds on the standard x86 runner, arm64 on
    # GitHub's free public-repo arm runner, and both legs run in parallel.
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux/amd64
            runner: ubuntu-24.04
            arch: amd64
          - platform: linux/arm64
            runner: ubuntu-24.04-arm
            arch: arm64
    permissions:
      contents: read
      packages: write
      id-token: write
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4.2.0

      - name: Log in to GitHub Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@abd2ef45e78c5afb21d64d4ca52ee8550d9572c7 # v4.5.1
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate labels
        id: meta
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302 # v6.2.0
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}

      - name: Build and push by digest
        id: build
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          context: .
          file: ./Dockerfile
          platforms: ${{ matrix.platform }}
          labels: ${{ steps.meta.outputs.labels }}
          # Per-arch cache scope: the native amd64 and arm64 runs must not
          # clobber each other's gha cache entries.
          cache-from: type=gha,scope=${{ matrix.arch }}
          cache-to: type=gha,mode=max,scope=${{ matrix.arch }}
          provenance: ${{ github.event_name != 'pull_request' }}
          sbom: ${{ github.event_name != 'pull_request' }}
          no-cache: ${{ github.event.inputs.force-rebuild == 'true' }}
          # On push/tag: push each arch by digest (no tag yet) so the merge
          # job can assemble the manifest list. On PRs: build only (cacheonly)
          # to validate both arches compile, producing nothing to push.
          outputs: >-
            ${{ github.event_name != 'pull_request' && format(
            'type=image,name={0}/{1},push-by-digest=true,name-canonical=true,push=true',
            env.REGISTRY, env.IMAGE_NAME) || 'type=cacheonly' }}

      - name: Export digest
        if: github.event_name != 'pull_request'
        env:
          DIGEST: ${{ steps.build.outputs.digest }}
        run: |
          mkdir -p /tmp/digests
          # Filename is the bare digest hex; the merge job reconstructs the
          # ${image}@sha256:${hex} ref from it.
          touch "/tmp/digests/${DIGEST#sha256:}"

      - name: Upload digest
        if: github.event_name != 'pull_request'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: digests-${{ matrix.arch }}
          path: /tmp/digests/*
          if-no-files-found: error
          retention-days: 1

  merge:
    name: Merge Multi-Arch Manifest
    # Assembles the per-arch digests into a single manifest list tagged with
    # the sha tag the downstream test/scan/promote jobs consume. Push/tag only;
    # PRs never push, so there is nothing to merge.
    needs: build
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: write
    outputs:
      # `tags` is a newline-separated list; the manifest-list step below
      # consumes all of it, but this output and the inspect step are
      # single-ref consumers (docker pull / Trivy / imagetools inspect), so
      # pin to the first tag to keep the contract unambiguous if a second
      # tag type is ever added to the meta block.
      image-sha-tag: ${{ fromJSON(steps.meta.outputs.json).tags[0] }}
      image-digest: ${{ steps.inspect.outputs.digest }}
    steps:
      - name: Download digests
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          path: /tmp/digests
          pattern: digests-*
          merge-multiple: true

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4.2.0

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@abd2ef45e78c5afb21d64d4ca52ee8550d9572c7 # v4.5.1
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate SHA tag
        id: meta
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302 # v6.2.0
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            # Build with SHA tag only - promote later after testing
            type=sha,prefix=sha-

      - name: Create manifest list and push
        working-directory: /tmp/digests
        env:
          IMAGE_FULL: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
        run: |
          refs=""
          for digest_file in *; do
            refs="$refs ${IMAGE_FULL}@sha256:${digest_file}"
          done
          tags=$(jq -cr '.tags | map("-t " + .) | join(" ")' <<< "$DOCKER_METADATA_OUTPUT_JSON")
          # Intentional word-splitting: $tags expands to "-t <tag>" pairs and
          # $refs to space-separated source digests.
          # shellcheck disable=SC2086
          docker buildx imagetools create $tags $refs

      - name: Inspect image
        id: inspect
        env:
          IMAGE_TAG: ${{ fromJSON(steps.meta.outputs.json).tags[0] }}
        run: |
          docker buildx imagetools inspect "$IMAGE_TAG"
          digest=$(docker buildx imagetools inspect "$IMAGE_TAG" \
            --format '{{json .Manifest.Digest}}' | tr -d '"')
          echo "digest=$digest" >> "$GITHUB_OUTPUT"
          echo "Manifest list pushed: $IMAGE_TAG ($digest)"

  test:
    name: Functional Testing
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: read
    needs: merge
    if: github.event_name != 'pull_request'
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@abd2ef45e78c5afb21d64d4ca52ee8550d9572c7 # v4.5.1
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull built image
        env:
          IMAGE_TAG: ${{ needs.merge.outputs.image-sha-tag }}
        run: |
          echo "Pulling image: $IMAGE_TAG"
          docker pull "$IMAGE_TAG"
          # Tag locally for testing
          docker tag "$IMAGE_TAG" nut-cgi:test

      - name: Create test hosts.conf
        run: |
          cat > hosts.conf << 'EOF'
          # Test configuration - no actual UPS required
          MONITOR testups@localhost "Test UPS System"
          EOF

      - name: Run container
        run: |
          docker run -d \
            --name nut-cgi-test \
            -p 8080:80 \
            -v "$(pwd)/hosts.conf:/etc/nut/hosts.conf:ro" \
            nut-cgi:test

      - name: Wait for container startup
        run: |
          echo "Waiting for container to be healthy..."
          timeout 30 sh -c "
            until [ \"\$(docker inspect -f {{.State.Health.Status}} nut-cgi-test)\" = \"healthy\" ]; do
              echo \"Container status: \$(docker inspect -f {{.State.Health.Status}} nut-cgi-test)\"
              sleep 2
            done
          "

      - name: Verify container health
        run: |
          status=$(docker inspect -f '{{.State.Health.Status}}' nut-cgi-test)
          echo "Container health status: $status"
          if [ "$status" != "healthy" ]; then
            echo "ERROR: Container is not healthy"
            docker logs nut-cgi-test
            exit 1
          fi

      - name: Test web server response
        run: |
          echo "Testing web server on port 8080..."
          curl -v --fail --max-time 10 http://localhost:8080/upsstats.cgi

      - name: Test HTTP headers
        run: |
          echo "Verifying HTTP headers..."
          headers=$(curl -sI http://localhost:8080/upsstats.cgi)
          echo "$headers"
          if ! echo "$headers" | grep -qi "Content-Type"; then
            echo "ERROR: Missing Content-Type header"
            exit 1
          fi
          if ! echo "$headers" | grep -qi "X-Content-Type-Options: nosniff"; then
            echo "ERROR: Missing X-Content-Type-Options header"
            exit 1
          fi
          if ! echo "$headers" | grep -qi "X-Frame-Options: DENY"; then
            echo "ERROR: Missing X-Frame-Options header"
            exit 1
          fi
          echo "HTTP headers validated successfully"

      - name: Test upsset.cgi is blocked by default
        run: |
          echo "Verifying upsset.cgi is blocked..."
          status=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/upsset.cgi)
          if [ "$status" != "403" ]; then
            echo "ERROR: upsset.cgi should return 403, got $status"
            exit 1
          fi
          echo "upsset.cgi correctly blocked (HTTP 403)"

      - name: Test CGI execution
        run: |
          echo "Testing CGI execution..."
          response=$(curl -s http://localhost:8080/upsstats.cgi)
          # Should have HTML content even without actual UPS
          if [ -z "$response" ]; then
            echo "ERROR: Empty response from CGI"
            exit 1
          fi
          echo "Response length: ${#response} bytes"
          echo "CGI execution verified"

      - name: Test health check script directly
        run: |
          echo "Testing health check script execution..."
          docker exec nut-cgi-test /healthcheck.sh
          exitcode=$?
          if [ $exitcode -ne 0 ]; then
            echo "ERROR: Health check script failed"
            exit 1
          fi
          echo "Health check script passed"

      - name: Test strict health check mode (should pass with basic infrastructure)
        run: |
          echo "Testing strict health check mode..."
          # This will fail because no actual UPS is configured, which is expected
          docker exec -e HEALTHCHECK_MODE=strict nut-cgi-test /healthcheck.sh || true
          echo "Strict mode test completed (expected to fail without real UPS)"

      - name: Verify non-root execution
        run: |
          echo "Verifying container runs as non-root user..."
          userid=$(docker exec nut-cgi-test id -u)
          username=$(docker exec nut-cgi-test id -un)
          echo "Container runs as: UID=$userid, User=$username"
          if [ "$userid" = "0" ]; then
            echo "ERROR: Container is running as root"
            exit 1
          fi
          echo "Non-root execution verified (UID: $userid)"

      - name: Test read-only filesystem compatibility
        run: |
          echo "Testing with read-only filesystem..."
          docker rm -f nut-cgi-test
          docker run -d \
            --name nut-cgi-test \
            --read-only \
            --tmpfs /tmp \
            -p 8080:80 \
            -v "$(pwd)/hosts.conf:/etc/nut/hosts.conf:ro" \
            nut-cgi:test

          # Wait for startup
          sleep 10

          # Verify it works with read-only filesystem
          curl --fail --max-time 10 http://localhost:8080/upsstats.cgi
          echo "Read-only filesystem test passed"

      - name: Show container logs on failure
        if: failure()
        run: |
          echo "=== Container logs ==="
          docker logs nut-cgi-test
          echo "=== Container inspect ==="
          docker inspect nut-cgi-test

      - name: Cleanup
        if: always()
        run: |
          docker rm -f nut-cgi-test || true
          rm -f hosts.conf

  scan:
    name: Security Scan
    runs-on: ubuntu-24.04
    needs: merge
    if: github.event_name != 'pull_request'
    permissions:
      contents: read
      packages: read
      security-events: write
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@abd2ef45e78c5afb21d64d4ca52ee8550d9572c7 # v4.5.1
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull built image
        env:
          IMAGE_TAG: ${{ needs.merge.outputs.image-sha-tag }}
        run: |
          echo "Pulling image for scanning: $IMAGE_TAG"
          docker pull "$IMAGE_TAG"

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          image-ref: ${{ needs.merge.outputs.image-sha-tag }}
          format: 'sarif'
          output: 'trivy-results.sarif'
          severity: 'HIGH,CRITICAL'
          exit-code: '1'

      - name: Upload Trivy results to GitHub Security
        if: always()
        uses: github/codeql-action/upload-sarif@e4fba868fa4b1b91e1fdab776edc8cfbe6e9fb81 # v4.37.3
        with:
          sarif_file: 'trivy-results.sarif'

      - name: Run Trivy for human-readable output
        if: always()
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          image-ref: ${{ needs.merge.outputs.image-sha-tag }}
          format: 'table'
          severity: 'HIGH,CRITICAL'

  promote:
    name: Tag and Promote Image
    runs-on: ubuntu-24.04
    needs: [merge, test, scan]
    if: |
      github.event_name != 'pull_request' &&
      needs.test.result == 'success' &&
      needs.scan.result == 'success'
    permissions:
      packages: write
    steps:
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@abd2ef45e78c5afb21d64d4ca52ee8550d9572c7 # v4.5.1
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Determine promotion tags
        id: tags
        env:
          SOURCE_TAG: ${{ needs.merge.outputs.image-sha-tag }}
          GIT_REF: ${{ github.ref }}
          GIT_REF_NAME: ${{ github.ref_name }}
          IMAGE_FULL: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
        run: |
          TAGS=""

          # Add :main tag for pushes to main branch
          if [ "$GIT_REF" = "refs/heads/main" ]; then
            TAGS="$TAGS $IMAGE_FULL:main"
          fi

          # Add release tags for version tags
          if [[ "$GIT_REF" =~ ^refs/tags/v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            VERSION="$GIT_REF_NAME"
            # Remove 'v' prefix
            VERSION_NO_V="${VERSION#v}"

            # Extract version components
            MAJOR=$(echo "$VERSION_NO_V" | cut -d. -f1)
            MINOR=$(echo "$VERSION_NO_V" | cut -d. -f2)

            # Add all version tags
            TAGS="$TAGS $IMAGE_FULL:latest"
            TAGS="$TAGS $IMAGE_FULL:$VERSION"
            TAGS="$TAGS $IMAGE_FULL:v$MAJOR.$MINOR"
            TAGS="$TAGS $IMAGE_FULL:v$MAJOR"
          fi

          echo "SOURCE_TAG=$SOURCE_TAG" >> "$GITHUB_OUTPUT"
          echo "PROMOTION_TAGS=$TAGS" >> "$GITHUB_OUTPUT"

          echo "Will promote:"
          echo "  From: $SOURCE_TAG"
          echo "  To: $TAGS"

      - name: Promote image with additional tags
        if: steps.tags.outputs.PROMOTION_TAGS != ''
        env:
          SOURCE: ${{ steps.tags.outputs.SOURCE_TAG }}
          PROMOTION_TAGS: ${{ steps.tags.outputs.PROMOTION_TAGS }}
        run: |
          # Use docker buildx imagetools create to add tags (fast, no rebuild)
          for TAG in $PROMOTION_TAGS; do
            echo "Promoting $SOURCE -> $TAG"
            docker buildx imagetools create --tag "$TAG" "$SOURCE"
          done

      - name: Promotion complete
        env:
          SHA_TAG: ${{ needs.merge.outputs.image-sha-tag }}
          PROMOTION_TAGS: ${{ steps.tags.outputs.PROMOTION_TAGS }}
        run: |
          echo "Image promotion successful"
          echo "SHA tag: $SHA_TAG"
          if [ -n "$PROMOTION_TAGS" ]; then
            echo "Promoted to: $PROMOTION_TAGS"
          else
            echo "No additional tags applied"
          fi

  # Stable-named aggregator so the branch ruleset can require a single
  # `ci-pass` check regardless of how the underlying matrix/conditional
  # jobs evolve. Treats SKIPPED as success: docs-only PRs intentionally
  # skip `build` (and therefore `merge`/`test`/`scan`/`promote`) via
  # `changes`, and PRs that do build still skip the push-only `merge`/`test`/
  # `scan`/`promote` jobs — the aggregator must not block those merges.
  # Mirrors the pattern used in owine/house-manager#191 and
  # owine/trip-tracker ci.yml.
  ci-pass:
    needs: [changes, build, merge, test, scan, promote]
    if: always()
    runs-on: ubuntu-24.04
    steps:
      - name: All required jobs passed or were appropriately skipped
        env:
          NEEDS_JSON: ${{ toJSON(needs) }}
        run: |
          # Log only the per-job `result` field, never the full `needs` blob.
          # `outputs` declared by upstream jobs can carry sensitive values
          # (image digests, tokens) so restricting the dump to results keeps
          # the diagnostic ("which job failed?") without the leak risk.
          echo "$NEEDS_JSON" | jq 'map_values({result})'
          # Whitelist acceptable results so future result types ('neutral',
          # 'action_required', etc.) fail closed instead of silently passing.
          if echo "$NEEDS_JSON" | jq -e 'any(.[].result; . != "success" and . != "skipped")' >/dev/null; then
            echo "::error::One or more required jobs did not succeed or skip."
            exit 1
          fi
